name: 'Build'
description: 'Compile to typescript and verify typescript definitions'
runs:
  using: 'composite'
  steps:
    - name: Compile to JavaScript
      run: npm run build
      shell: bash
    - name: Verify generated Typescript definitions
      run: npx attw --pack .
      shell: bash
