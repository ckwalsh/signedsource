name: PR for main
on:
  pull_request:
    branches:
      - main
jobs:
  build-and-test:
    name: Build and Test
    uses: ./.github/workflows/build-and-test.yml
  echo-event:
    runs-on: homelab
    name: Dump event
    steps:
      - run: cat ${{ github.event_path }}
