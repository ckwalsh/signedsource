name: PR for main
on:
  pull_request:
    types: [opened, synchronize, reopened]
    branches:
      - main
jobs:
  build-and-test:
    name: Build and Test
    uses: ./.github/workflows/build-and-test.yml
