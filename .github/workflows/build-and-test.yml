name: 'Build and Test'
on:
  pull_request:
  push:
    branches:
      - main
jobs:
  test:
    runs-on: homelab
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version: 'lts/*'
      - run: |
          npm ci
          npm run typecheck
          npm run lint
          npm run test
          npm run build
          npx attw --pack .
